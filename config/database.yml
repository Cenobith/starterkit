# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: 5
  timeout: 5000

local: &local
  adapter: postgresql
  username: postgres
  password: pAsSwOrD
  host: localhost
  port: 5432

development:
  <<: *local
  database: starterkit_dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *local
  database: starterkit_test

# You can delete this if you will deploy your app to Heroku
production:
  <<: *local
  database: starterkit